
<div class="main-content">
  
    <div class="card  p-4 mx-auto" style="max-width:1300px;">
      

      <div class="text-center mb-4">
        <img  id="fotoPerfil"  src="https://cdn-icons-png.flaticon.com/512/1077/1077012.png"
             class="rounded-circle border" width="100" height="100">
             <div class="" style="justify-content: center; object-fit: cover;">
       </div>
       <button  type="button" class="btn btn-success px-4"  onclick="document.getElementById('Byimagen').click()">
          Subir una imagen
       </button>

       <input type="file" id="Byimagen" accept="image/*" hidden onchange="Most(event)">
      </div>
      

      <?php if (session()->getFlashdata('msg')): ?>
        <div class="alert alert-success text-center"><?= session()->getFlashdata('msg') ?></div>
      <?php endif; ?>

      <form action="<?= base_url('perfil/actualizar') ?>" method="post">
        <input type="hidden" name="id_usuario" value="<?= esc($perfil['id_usuario']) ?>">
        <input type="hidden" name="id_persona" value="<?= esc($perfil['id_persona']) ?>">

        <div class="mb-3">
          <label class="form-label">Nombre</label>
          <input type="text" class="form-control" name="nombre" value="<?= esc($perfil['nombre']) ?>">
        </div>

        <div class="mb-3">
          <label class="form-label">Usuario</label>
          <input type="text" class="form-control" name="username" value="<?= esc($perfil['username']) ?>">
        </div>

        <div class="mb-3">
          <label class="form-label">Contraseña</label>
          <input type="password" class="form-control" name="password" placeholder="••••••••••">
          <small class="text-muted">Deja vacío si no deseas cambiarla</small>
        </div>
 
        <div class="d-flex justify-content-center gap-3 mt-4">
          <?php
           $lastPage = session()->get('last_page') ?? 'dashboard'; 
          ?>
           
          <button type="submit" class="btn btn-success px-4">Guardar cambios</button>
        </div>
      </form>
    </div>
  
    <script src="js/subirimage.js"></script>
 
</div>